# 任务卡 <ID>:<标题>

- 状态:待领取 ｜ 实现:<Implementer 身份> ｜ 出卡/评审:<Architect 身份>
- 对应 backlog:<里程碑 · 批次 · 任务号>
- 前置任务:<已合入的前置卡列表;无则写"无">
- 工作区/管理者:<A / B / C;manual / multica;项目部署配置路径>
- 分支:<手工创建时填写指定分支;宿主已分配时沿用实际分支,不得重建;并行卡独立分支>
- 任务基线 `task_base`:<确定 commit SHA;卡面授权的依赖同步 SHA,无则写无>
- C / 托管交付:<运行开始后记录 run_id/run_start/实际分支;交付时附 delivery_sha 与最终 guard 检查结果>
- C 消息与集成:<消息 task 命名空间、发送分支登记方式、唯一集成人;详情引用部署配置>
- **验收强度**:<常规 ／ 对抗>(protocol §2.2)——<一句话理由,回答"这段代码面对谁">
- **体量预授权**:本卡手写 gross 上限 <预算 × 1.3;穷举型写"不设限,由强制交付面决定">;对抗档把验收面单独列出,不要并进主逻辑估算

## 背景(为什么做)

<一段话 + 设计文档章节指针,如 `xxx-design.md §2.1`。数据面/前置产物就绪状态在此说明。>

## 目标(做成什么样)

<可观察的结果描述,不写实现。多目标拆多卡。>

## 涉及范围

- **允许**:<白名单式列出目录/文件>
- **禁止**:<明确列出;默认"其余一切文件">;零新增第三方依赖(确需则本卡显式批准并列 companion 闭包)

## 接口契约

<函数签名/类型/命令名与参数——Architect 给定,Implementer 不得擅改。与 schema 逐字段对照后写入。无跨模块接口则写"无新增契约"。>

## 实现指引

<关键思路、指定库、坑位提醒;不写伪代码全文,留实现空间。绿区自选项在此显式标注(如"选型 X 或 Y,理由写交付报告")。>

## 验收标准(DoD)

- [ ] <项目门禁命令表全绿>
- [ ] <C / 托管任务:当前运行的目录/分支未变,run_start 是交付 HEAD 的祖先;已提交独立消息,给出消息 ID 与载体 SHA>
- [ ] <功能验收逐条具体、可判定——"功能正常"不算>
- [ ] 新增逻辑有测试;不降低既有测试通过率
- [ ] <真机实测路径(需要时):操作序列 + 预期结果 + 截图要求>
- [ ] <UI 类:双主题/可达性等项目基线合规>

## 边界与禁区

- <本卡明确不做的事及其归属(后续卡/其他里程碑),防扩范围>
- 遇到卡面与实际代码矛盾:停工 L1 上报,不自行"顺手修"

## 交付物

交付报告(信道):做了什么/验证方法/体量分组统计/自主决策节/实施发现 L1-L3(无则写"无")/存疑点。
