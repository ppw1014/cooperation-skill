# 共同协议(Common Protocol)

> 本文件是双 agent 协作的共同底座,Architect 与 Implementer 都必须完整遵守。
> 角色专属操作手册:architect.md / implementer.md(项目实例中以部署路径为准)。
> 历史教训:anti-patterns.md。本协议源自项目 A 的协作规范 v1.5 及其实践演化。

**每轮开工先确认工作区**(§4),再执行角色手册。形态 C 或 Multica 托管任务必读 [本地 worktree 工作流](references/local-worktrees.md);托管任务先记录本轮起点,再做任何 Git 写操作。旧部署中的 A/B 标签不能覆盖实际拓扑与宿主分配。

## 1. 角色分工

| 角色 | 承担者 | 职责 | 专属权力 |
| --- | --- | --- | --- |
| **Owner** | 人类 | 方向决策、产品验收、真机测试、花钱的事 | 最终决策权;信道触发权(零内容搬运) |
| **Architect** | agent 甲 | 出任务卡、定接口契约、终审验收、裁决实施发现、维护 backlog 与设计文档 | 契约制定权、裁决权、merge 权 |
| **Implementer** | agent 乙 | 按任务卡实现、自检、交付报告、上报实施发现 | 绿区自主决策权(见 §6);无收口合入主干权 |
| **Reviewer**(可选) | agent 丙 | 开工前评审设计文档与高风险卡面;交付后初审实现,打回或放行 | 放行/打回权(**仅限有卡面依据者**);**无契约修改权、无 merge 权** |

**权力铁律**:契约 Architect 定;发现 Implementer 报;质量 Reviewer 先筛,**但筛不改契约**;裁决之前一律按契约实现。任何一环越权都是协作事故。

**Reviewer 是可选角色**——只有两个 agent 的项目不部署它,其职责全部回落 Architect(即本框架的双 agent 原形)。是否部署在信道存档中声明。

Owner 不搬运内容:agent 之间的沟通全部走信道(§3),Owner 只用固定短语触发(如"看信道"),需要 Owner 决策的事项(产品分歧、范围变更、花钱、安全敏感)不走信道,直接找 Owner。

### 1.1 Architect 代行与交接(限额 / 中断时)

Architect 会因限额、上下文耗尽或模型切换而中断。**没有代行协议,协作要么停摆、要么即兴处理,后者留下的是权责不清的半途卡。**

**谁可以代行**(按优先级):

1. **Reviewer**(三方部署时的默认代行者)——她已在读契约产物,上下文最近;
2. 另一个具备 Architect 能力的 agent(对等形态,见 §1.2);
3. 都没有 → **不代行**,在途卡停在当前状态等原任回归。

**绝不由 Implementer 代行裁决**——她随后就要按这些裁决实现,自己裁自己的活,裁决环节的全部价值归零。双 agent 项目的 Architect 中断时,正确做法是停在原地等,而不是让实现方顶上。

**代行期的权力边界**:

| 权力 | 代行者 | 说明 |
| --- | --- | --- |
| 裁决 L1 / 修订卡面 | ✅ | **即时生效且长期有效**,不因原任回归而重审 |
| 出卡、派卡 | ✅ | |
| 交付验收 | ✅ | 但只具**初审效力**,见下 |
| **收口合入主干** | ❌ | 代行期一切工作与提交**只在本地分支** |
| push / 远程操作 | ❌ | 仍按项目既有的 Owner 授权规则,代行不放宽 |

**与 §1 角色表的关系**:表里写 Reviewer「无契约修改权、无 merge 权」——代行**只临时解除前者,后者始终成立**。这不是巧合:merge 权正是这套机制的兜底所在,一旦下放,原任回归后的独立验收就没有了可挽回的对象。

**为什么裁决算数、收口不算**:裁决是**在途决策**,停工中的实现方等不起,事后重审的代价是整条依赖链;收口是**不可逆的基线变更**,它等得起,而原任回归后的独立完整验收是这套机制唯一的质量兜底——把兜底放在可逆的那一侧。

**待终审队列与链式基线**(这条决定「不合主干」会不会阻塞开发):

- 代行期验收通过的卡进入**待终审队列**:分支保留、不合主干、在信道登记;
- **后续卡以队列中最近一个待终审分支为基线拉新分支**,不等主干。卡面必须写明基线 commit,不写「基于 main」;
- 形态 C 用该基线的确定 SHA;Multica 在分配 worktree 前选好基线。若已开跑,按 §4 保留本轮起点,仅合并卡面授权的依赖,不重置分支去套基线;
- 原任回归后按队列**顺序**独立完整验收,通过则依次合入主干;
- **代价要认**:某卡终审不通过时,它与其全部下游一起返工。因此代行期应优先派**相互独立**的卡,并把队列长度控制在可承受的返工放大内;队列每增长一张,在信道报一次当前长度。

**交接对账**:原任回归时,代行者在信道发一条交接消息,列全代行期的**裁决编号、待终审队列(分支 + 基线 commit)、未闭合的 L1**。原任据此接手,不重读全部历史。

**半途卡不换手**:正处在裁决 / 返工循环中的卡,由代行者主持到该卡收口(或进入待终审队列),原任接回全局卡序与新卡。判据是**证据链在谁手上**——换手要求新任重读全部原始证据,收益为负。

### 1.2 角色的模型来源要记入存档

三个角色可能由同一个模型的不同实例承担。**同源实例的盲区是相关的**——它们会在同一个地方一起看不见,而流程照跑、成本照付。部署时把三方各自的模型来源记入信道存档(SKILL.md 部署流程第 1 步),并按下表调整预期:

| 组合 | 后果 |
| --- | --- |
| Reviewer 与 **Architect** 异源 | reviewer.md §1/§2 的开工前评审价值最高——**这是该角色性价比最高的部分** |
| Reviewer 与 Architect **同源** | 开工前评审收益显著下降;保留 §3 的冷读者初审即可,其价值来自上下文独立,与同源无关 |
| Reviewer 与 Implementer 同源 | 初审仍有效(冷读者视角),但别指望靠直觉找 bug 跑赢她 |

**对等形态(异源双 Architect)**:两个异源 agent 都持出卡与裁决能力,互为对方产出的审查方,其中一个承担日常出卡、另一个承担全局卡序与终审。这不是 §2.1 的「对等互审」**环节**,而是**常驻形态**——它同时满足跨模型正交与代行需求(§1.1),是三方部署之外的一个实际可选项。**已经是对等形态时,再加一个与二者之一同源的 Reviewer,边际价值很低。**

## 2. 任务生命周期

```
backlog 取任务 → Architect 出任务卡
  →〔高风险卡:Reviewer 预审卡面(§2.1,一轮止损)→ Architect 修订〕→ 信道派卡
  → Implementer 领卡拉分支 → 实现 + 自检清单 → 信道交付报告(附实施发现)
  →〔Reviewer 初审(强度按卡面档位,§2.2):放行 ／ 打回 Implementer(≤2 轮)／ 升级 Architect〕
  → Architect 终审验收(五步全量,不因初审而精简;代码 + 运行 + 逐条处理发现)
  → 通过:收口合入主干
     不通过:打回 Implementer(≤2 轮,返工后直接回 Architect 复审,不再过 Reviewer)
     两轮仍不过:Architect 接管重写
```

〔〕内为部署 Reviewer 时的环节;未部署则由 Architect 自行承担。**实现阶段 Reviewer 全程不介入**:期间的 L1 阻塞直达 Architect 裁决,裁决结论抄送 Reviewer(§5)。

开工前的两个〔〕环节受 **§2.1 的一轮止损与越线信号**约束;交付后的初审受 **§2.2 强度分档**约束——两处都没有自然终点,不设闸就会自己长出往返。

任务卡是**唯一事实源**:自包含 + 硬边界 + 机器可验收 DoD。派活提示词只指向卡面路径,不复制内容。

### 2.1 开工前交叉 review(可选环节,一轮止损)

契约产物(设计文档、冻结规格、高风险卡面)在**派出实现卡之前**过一次第二双眼睛。两种组织方式,**纪律完全相同**:

- **对等互审**:两个 agent 都能当 Architect,双方互审对方的产物;
- **常驻评审**:部署了 Reviewer 角色(§1),由她审 Architect 的产物——这就是 Reviewer 的开工前职责(reviewer.md §1、§2)。

理由是这个阶段的错误最贵——冻结文档一旦被实现,纠正成本从"改几行 Markdown"变成"返工整条依赖链";而单个 Architect 对自己刚写完的设计最难发现同名异义、接口空洞和未验证断言。

**这个环节的价值高度集中在前几轮,之后急剧转负,因此必须带止损规则。**

**一轮止损**:审方一次性给全所有问题,出卡方修完即固定基线并派卡,不再往返。只有一种情况允许追加一轮:**修复本身引入了新的硬伤**,且必须指名是哪条、为什么落在下面的必审清单里。

**必审(只有这四类)**:

| 类型 | 判定 |
| --- | --- |
| **假绿** | 某条验收在错误实现下同样通过——判据证明不了它声称的事、断言的是常量、检查对象为空时也成立 |
| **契约冲突** | 两份文档对同一事实、同一路径的写权限或同一名词说了相反的话 |
| **接口空洞** | 签发了令牌/ID/句柄却没有消费方,或反过来 |
| **未验证断言当事实** | 第三方库默认值、外部产物文件名、宿主能力、工具标志语义,凡"凭印象"的一律要求给来源或实测 |

**不审(审出来也不改,留到实现时暴露)**:表述是否自洽、数量词、小节交叉引用、验收样例覆盖得全不全、命名与体例、预算估得准不准。这些的修复成本在实现阶段和出卡阶段是一样的,但在出卡阶段要多花一整轮往返。

**四个越线信号,出现任一条立即收口派卡**:

1. 本轮提出的问题里,**多数是上一轮修订产生的**——已经在自己制造工作,不是在发现问题;
2. 问题开始集中在表述、数量词、交叉引用——必审四类已经审干净了;
3. 为**一张卡**产出的反模式条目数,超过该卡的章节数——标准定得比卡本身还重;
4. 互审累计耗时超过这张卡的预计实现耗时——投入产出已经倒挂。

审方与被审方都有义务喊停,不是只有被审方能喊。**"再看一眼总没坏处"是这个环节最主要的失效方式**:它没有自然终点,每一轮都能挑出东西,而挑出东西不等于值得挑。

### 2.2 验收强度分档

**强度由"这段代码面对谁"决定,不由"它有多重要"决定。** 出卡时在卡面标一档,backlog 同步。

| 档 | 适用 | 威胁模型 | 验收动作 |
| --- | --- | --- | --- |
| **常规** | 只处理项目自己产生的输入:工程基座、内部 schema、状态机、计算逻辑、内部工具、CI | 写错了 | 跑通门禁 + 正反例测试 + 契约一致 |
| **对抗** | 处理外部或不可信输入:用户上传、第三方 API 响应、网络数据、渲染进用户界面的内容、安全边界本身 | 被攻击 | 常规全部,外加恶意输入矩阵、边界绕过、失败关闭证明 |

多数项目里对抗档只占少数几张卡,**出卡时就要点名是哪几张**,不要留给验收时临场判断。

判据是一个具体问题:**谁会攻击这段代码?** 一个只有团队自己会改的内部文档检查脚本,答案是"没人"——给它设计"有人故意插入诱饵内容骗过检查"这类反例,是在虚构一个不存在的对手。反过来,处理用户上传文件的解析器答案是"任何拿到上传口的人",恶意输入矩阵一条都不能省。

**给常规档做对抗验收不是"稳一点"。** 它挤占的是真正需要对抗验收的那几张卡的预算,而且会让常规卡的体量翻倍——被审方为了通过而堆防御代码,审方为了发现问题而构造更刁钻的场景,双方都在加码,没有终点。

改档由 Architect 在信道说明理由并更新 backlog,Implementer 不自行升降。

#### 取证档(正交于上面两档,不是第三个强度)

上面两档问的是「谁会攻击这段代码」。**当卡要修的缺陷根因未知时,还要问第二个问题:「根因是不是已经由证据闭合?」** 答案为否时,这张卡的产出是**证据,不是修复**——它与常规/对抗档正交,两者都可能同时叠加取证档。

| 项 | 取证档的差异 |
| --- | --- |
| DoD | 不是「缺陷已修复」,而是「根因已由证据闭合,且证据形态满足卡面列举的 N 项」 |
| 交付物 | 诊断代码 + 原始证据;诊断代码可以是一次性的(测试态、取证后删除) |
| 体量 | 按诊断代码单独预算,不与后续修复共用额度 |
| 硬边界 | **证据闭合前禁止进入生产修复**,包括「顺手改一下」 |
| 分阶段 | 证据若需多轮才拿得到(必须跑特定平台、特定负载、远程环境),按阶段冻结:每阶段独立体量与 checkpoint,上一阶段证据不闭合不进下一阶段 |

**为什么值得单列一档**:跨平台、并发、环境相关的缺陷几乎都是这个形态,而它们最贵。跳过取证直接修,写出来的是**「在错误根因假设下恰好让测试变绿」的修复**——门禁不会告诉你这件事,它只会变绿。

出卡时若根因写的是推断而非实测,**卡面必须明说这是推断**,并把「实测根因与推断不符即 L1 停工」写进硬边界;否则实现方会把推断当契约照着实现。

## 3. 信道协议

形态 A/B 默认通过项目仓库内的单文件信道异步通信(默认 `docs/collab/channel.md`)。本节的单文件追加、全局编号规则适用于串行发言。

**形态 C 使用独立消息文件**(默认 `docs/collab/messages/<task>/<run-id>/<seq>.md`),部署配置中登记发送分支,按确定 SHA 读取;用唯一消息 ID、`in_reply_to`、`ack` 表达引用与已读,不使用共享 index 或全局 `#N`。旧 `channel.md` 保留为历史归档。具体收发步骤与模板见 [本地 worktree 工作流](references/local-worktrees.md#消息收发)。平台消息入口可作通知,不会自动替代文件交付。

### 3.1 基本纪律

- **只追加**:消息永不修改、永不删除。
- **写入方式**:追加自己的消息一律用**仅追加写入**(如 `cat >> <信道文件> <<'EOF' ... EOF`,或等价的"文件末尾插入"),不用需要整篇读入重写、或对全文做模糊匹配的工具动这份文件——文件随协作推进越来越长,整篇重写/模糊匹配在长文件上容易匹配错位或误伤历史消息,物理上保证"只追加"落到实处,而不只是口头纪律。
- **写入点只能由工作区文件本身决定**,不能用任何 git 视角。形态 A 下"自己的消息不 add"(§3.3)使**暂存版与 HEAD 版永远短于工作区文件**——照它们判断末尾,就会把消息写到别人未暂存消息的**前面**。`git diff HEAD -- <信道文件>` 只用于**读增量**,绝不用于**定位写入点**。
- **写入后自验(强制)**:上面两条是禁令,禁令依赖执行者记得;这三条是当场能发现的断言,追加完立刻跑,任一条不过就地修正:

  ```sh
  tail -3 <信道文件>                              # 你的消息必须在物理末尾
  grep -c '^### \[#<N>\]' <信道文件>               # 必须为 1(标题前缀按项目实例调整)
  git diff -- <信道文件> | grep -c '^-[^-]'       # 必须为 0:没动过任何历史内容
  ```

- **万一写错位置**:已写入的消息**保持原地不动**。重排违反只追加;重发会制造重复编号,那比错位更糟——**编号唯一性是引用的基础**,而物理顺序错了还能靠编号还原。正确处置是在下一条消息的元信息里注明「注:#N 物理位置早于 #M,按编号读」。只有**内容不完整或被截断**才需要重发,且必须用**新编号**,正文说明它是哪一条的完整重发。
- **编号**:消息编号 `#N` 全局严格递增,回复引用编号(如"就 #12 的 L1")。
- **元协议豁免**:信道消息豁免任务卡的实现范围限制;形态 C 仅允许在本方消息命名空间新增文件,不能借此改他人消息或部署配置。
- **单一事实源**:信道里不复述卡面/文档内容,只给路径与结论。

### 3.2 消息格式(三方寻址)

```
## #N ｜ <发件人> → <收件人> ｜ <日期> ｜ <主题>
> 抄送:<角色,无则写"无"> ｜ 已读至:#M ｜ 下一步:@<角色>
```

- **收件人唯一**——是这条消息要求**动作**的那一方;需要知情但不需要动作的一律写进**抄送**。
- **已读至:#M**:声明本方已读到的最高编号。三方场景下这是唯一可靠的已读水位线(见 3.3)。
- **下一步:@<角色>**:明确下一个该动的人。Owner 读最后一条消息即知道该触发谁,不必回溯全流程——三方协作下 Owner 的触发次数翻倍,这一行是给人省事的。
- **消息类型**:派卡 / 卡面预审 / 文档评审 / 交付报告 / 初审结论 / 打回 / 升级 / L1 裁决请求 / 裁决 / 验收结论 / 收口通知 / 技术问答 / 形态探测。

### 3.3 已读机制(先按工作区形态分流)

以下 staging 回执**只适用于形态 A 的同一 index**。形态 B 在已提交消息中显式写 `已读至`;形态 C 用 `ack` 列出实际读过的消息 ID。不同 worktree 的 `git add` 不会通知对方。

- **两方**:`git add <信道文件>` = 已读回执——只 add 含对方消息的状态,**自己写的消息不 add**(留给对方 add 作回执);回复对方之前先 add 对方消息。
- **三方**:`git add` 是**单一全局状态,表达不了"谁读了"**——主收件人一 add,抄送方的未读增量就从 `git diff` 里消失了。故:
  - `git add` 语义收窄为**主收件人的已读回执**,只有 `→` 指向的那一方 add;
  - **抄送方不 add**,且读增量必须用 `git diff HEAD -- <信道文件>`(而非 `git diff`——后者看不到已被主收件人 staged 的消息);
  - 真正的已读水位线是元信息行里的 **`已读至:#M`**,每方发言时如实声明,不依赖 git staging 状态。
- **提交时 `git commit` 也必须带 pathspec**——`git add <路径>` 精确暂存**还不够**:不带 pathspec 的 `git commit` 提交的是**整个 index**,而形态 A 下 index 里往往有**对方 add 的信道回执**(上面这条协议要求的动作),于是你的一次文档提交会把别人的已读状态连带提交进去。写成 `git commit <明确路径…>`,或提交前跑一次 `git diff --cached --stat` 确认 index 只含你要提交的文件。
  - 误提交时先确认工作区与运行起点。非托管形态 A 只有在确认无他人现场、且该提交未交付时才考虑回退重提;托管任务追加更正,不得套用 `reset --soft HEAD~1` 改写起点。仅取消暂存的 `git restore --staged <路径>` 不移动 HEAD,与历史回退不同。
- 例外:收口合入时 Architect 可将信道记录一并 commit。

## 4. 工作区形态(A/B/C)与宿主约束

工作区拓扑与宿主管理是两件事。部署配置分别记录**形态 A/B/C**和**管理者 manual / multica**;同一项目可同时存在共享源目录与多个任务 worktree,按实际参与者安排分流。

### 形态 A:共享本地工作区

两个 agent 读写**同一个本地目录**。未提交的变更互相可见;信道消息写入文件即送达。

- 切分支 / merge / checkout / reset 之前,**必须确认对方无未提交现场**(信道询问或 Owner 确认)——这类操作会摧毁对方工作区状态,是 A 形态第一铁律。
- push 只按 Owner 明确要求执行(收口时按项目惯例)。
- 消息交付无需 commit;各自的消息随各自的下一次正常提交入库(Architect 随主干提交,Implementer 随任务分支提交,merge 汇流)。

### 形态 B:分离工作区

各自 clone,一切经 commit + push 可见。

- 信道消息必须 **commit + push 后才算送达**;读前 fetch,用远端的确定 SHA 读取。更新当前分支时显式选择保留本轮起点的方式,不用可能隐式 rebase 的裸 `git pull`。
- 交付 = push 任务分支 + push 信道消息;领卡前先拉取主干最新。
- 无"对方现场"问题,但**远端分支是唯一事实**,本地状态不作数。

### 形态 C:本地并行 worktree

- 各自 `toplevel` / `git-dir` 不同,`git-common-dir` 相同。工作文件、index、HEAD 独立;对象库和普通分支引用共享。别人的提交可直接按 SHA 读,未提交文件和 staging 回执不可见,不需要为了本地交付 push。
- 每个运行只写自己的 worktree 与分支。共享引用意味着不能强移、删除他人的分支,也不能用 `git -C <源目录>` 修改源工作区来绕过本方约束。
- 并行卡分配独立分支与消息命名空间,范围和依赖在卡面列明。已授权依赖可按确定 SHA 合并进自己的任务分支;这是基线同步,不是收口合入主干,后者仍归指定 Architect。
- 交付包含 `task_base`、`run_start`、`delivery_sha` 和本地验证证据。Reviewer 固定到交付 SHA;指定 Architect 串行集成,不以移动中的分支名代替已验收提交。
- 配置、消息、评审与收口操作见 [本地 worktree 工作流](references/local-worktrees.md)。

### Multica 托管任务(叠加在实际拓扑上)

- **使用平台已分配的目录、分支和 HEAD 状态**,不按模板重新建分支、切 main、重建 worktree。每轮在任何 Git 写操作前执行 guard `start`,冻结 `run_start`;它不是任务最初的 `task_base`。
- **交付 HEAD 必须包含本轮起点**:`git merge-base --is-ancestor <run_start> HEAD` 必须成功。默认追加提交;不 amend 起点或更早提交、不 rebase 掉起点、不 reset 到其历史之外。平台的 baseline/checkpoint 提交即使说明不符合署名规范也保留原样。
- `start` 同一运行只能捕获一次,上下文恢复与重复检查使用原 `run_id`。真正的新一轮使用新的运行 ID,不能为了让检查通过更换 ID 或覆盖记录。
- 分支交付、平台记录成功、主干集成是不同状态。平台记录成功前不清理任务分支/worktree;托管资源由平台回收。失败时保留起点、交付 SHA 与现场,按参考文档诊断。

### 三方部署时 Reviewer 的工作区(形态 A 下二选一,记入信道存档)

初审要跑门禁,会写出构建产物、可能装依赖——在共享工作区里这会和 Implementer 的现场互相污染。两种解法:

- **A-共享**:三方同目录。新增交接铁律——**Implementer 交付时必须声明「已停手、现场已 commit 到任务分支」,Reviewer 才开始初审;初审期间 Implementer 不动工作区**。零额外设施,靠纪律约束。
- **A-独立审**:Reviewer 用独立 worktree 或 clone,只读代码 + 跑门禁,不碰共享工作区。它与实现方之间的通信须分别按 C 或 B 交付,不能继续使用共享 index 回执。**注意**:worktree 里的 `.git` 是文件而非目录,挂载、拷贝进沙箱时须保留它引用的 Git 元数据可达性。

形态 B 下三方各自 clone,天然无此问题。

### 形态确定方式

1. 读取宿主本轮分配与项目部署配置;旧标签与实际不符时由配置维护者修订,其他角色报告差异并保留现场,不能靠切分支或 reset 把现场改成文档声称的样子。
2. 各方执行 `git rev-parse --path-format=absolute --show-toplevel --git-dir --git-common-dir`(已部署 guard 时也可用 `inspect`),比对规范化路径与所在机器:同机同 toplevel/index 是 A;同机不同 toplevel、相同 common-dir 是 C;独立对象库的 clone 是 B。不同机器上的同名路径不代表共享仓库。
3. 拓扑确定后验证消息通路:A 读未提交标记;B 从已 push 的确定 SHA 读;C 从另一任务分支的确定 SHA 读独立消息。**读不到未提交消息不能据此判 B,也不自动授权 push**。
4. 将拓扑证据、管理者、消息分支、集成人与主干入口记录在部署配置中。无法确认的参与者按未知处理,先只读核查,不操作其工作区。

## 5. 实施反馈分级(L1 / L2 / L3)

实施中**主动报告发现是任务的一部分**,不是可选动作——但报告与裁决分离,**发现不等于授权**。

| 级别 | 定义 | Implementer 的动作 |
| --- | --- | --- |
| **L1 阻塞** | 契约无法按写实现:指定版本不存在、API 与文档不符、平台限制、两份文档互相矛盾 | **立即停工**,信道上报并等待裁决;不自行变通、不绕过 |
| **L2 缺陷发现** | 设计/文档的错误、隐患、遗漏场景,但不阻塞当前任务按契约完成 | **继续按契约实现**,发现随交付报告"实施发现"一节结构化提出;若会让当前实现方向性做错,升级为 L1 |
| **L3 改进建议** | 更优的库/更简的实现/体验或工程化改进机会 | 同 L2 列出,标注"仅建议";**裁决前不得按建议实现** |

**统一反馈格式**(每条独立成项):

```
[L1/L2/L3] 现象:<撞上了什么,一句话>
影响:<影响哪个文档/模块/后续任务>
证据:<报错原文/官方文档链接/复现步骤>
建议:<你认为的修法,可空>
```

**铁律**:无论发现什么,裁决之前一律按任务卡契约实现。"我发现了更好的方式所以直接改了"是本协议最严重的违规——发现的价值在于进入裁决流程,不在于绕过流程的"好心"。

**Architect 对应义务**:review 时必须逐条处理实施发现(采纳→改文档/开任务/挂账;否决→给理由),不允许无回应地合并。

**裁决必须抄送 Reviewer**(部署时):裁决往往修订契约,Reviewer 没收到就会**按旧卡面初审、成批误打回**。同理,卡面的任何修订都要抄送——Reviewer 手里的卡面版本必须与 Implementer 手里的一致,否则初审环节反成噪声源。

**裁决是唯一没有交叉检查的契约变更点。** 出卡要过预审(§2.1),裁决却不过——而裁决的时间压力更大(实现方停工等着)、可用上下文更少。让每个 L1 都过一遍预审会拖死停工方,不可取;零成本的补法是**把核对挪到初审**:

- 裁决若**改动了 DoD 或涉及范围**,Reviewer 初审时有义务专门核对该处——不是重新裁决,而是确认「按新契约实现出来的结果」与「裁决当时想要的结果」一致;
- 该项发现的问题按 §6 分流:指得到卡面(含裁决后的卡面)某一行 → 打回 Implementer;指不到 → 是裁决本身的问题,升级 Architect。

## 6. 决策权分区(绿区 / 红区)

> 背景:源项目实践中,五次 L1 停工的实现侧首选方案与最终裁决 5/5 一致,裁决环节零信息增量。故把低风险"必然配套"决策下放实现侧,L1 留给真正需要第二双眼睛的事。

**绿区(Implementer 可不停工自主决定)**——同时满足全部四条件:

1. 是任务卡强制目标的**必然配套**——不做则 DoD 无法达成;
2. 不改变**运行时依赖面**与**公共 API/契约形状**;
3. 改动可精确限定到具体文件与字段,不夹带无关修改;
4. 有 dry-run 或等价的**只读验证证据**(先证明可行再动手)。

绿区典型范围(枚举,超出即回落 L1):纯类型/测试工具类 devDependencies(精确版本锁定,与仓库既有同名包同版本);测试域配置字段(不影响产物);package 非运行时字段(入口与构建管道);新增测试 fixture。

自主决策的义务:交付报告设**「自主决策」独立一节**,逐项按 L1 同等证据格式披露(现象/依据/证据/diff 边界);review 逐条审计,被否决项返工,不影响其余交付。

**红区(一律 L1 停工,无例外)**:新增运行时依赖;公共契约形状变更(DTO、schema 语义、包导出面);修改已验收的跨批产物;CI/workflow 结构;安全边界(进程执行、文件系统范围、网络访问);删减既有测试覆盖;与 ADR 或既有裁决冲突;任何方向性选择。

**最小必要例外**(L2 披露,不停工)——已授权目标的必然附带变更,同时满足:①目标的直接机械结果(如已授权依赖引发的 lockfile 对应锁定);②可精确限定到具体文件与内容,不夹带;③不改产品/架构语义、公共契约、CI 策略、任务拆分策略;④不与既有裁决冲突。交付报告中作为 L2「最小必要例外」列明证据与 diff 边界。

### 评审边界(部署 Reviewer 时适用)

Reviewer 有放行/打回权,**没有契约修改权**。判据只有一条,机械可验:

> **打回项必须能指到卡面某一行**——DoD 第几条 / 接口契约哪一行 / 涉及范围哪一项。

- 指得到 → 契约内缺陷,打回 Implementer;
- 指不到但确有问题 → **是卡面本身的问题**,走 Architect 裁决;不打回、不自行要求 Implementer 改;
- 指不到且只是"我会写得更好" → 作为建议随放行意见交 Architect,**不构成打回项**;
- 拿不准属于哪种 → 按"卡面问题"上报 Architect。保守方向是惊动 Architect,而不是越权改契约。

**Implementer 的对称义务**:收到**无卡面依据**的打回项,有权拒绝并上报 Architect 裁决,不得默默照改——照改等于让契约在信道里被静默改写,是三方协作下最隐蔽的契约漂移路径。

**轮次预算**:Reviewer 初审最多打回 2 轮,仍不过则升级 Architect(附初审记录与分歧点),不得继续打;Architect 终审打回照旧最多 2 轮,且返工后直接回 Architect 复审、不再过 Reviewer。一张卡的打回总轮次因此封顶在 4。

**初审强度随卡面档位**(§2.2):常规档卡按常规档初审——门禁复跑、契约一致、DoD 落点即可。**不要给常规档卡做对抗式初审**:给每张卡都上满清单,和 §2.1 里"再看一眼总没坏处"是同一种失效,只是搬到了交付后;而常驻 Reviewer 比一次性互审更容易犯,因为她每张卡都在场。

**§2.1 的越线信号同样适用于初审↔返工循环**,其中这两条最灵:①本轮打回项多数由上一轮返工产生——在自己制造工作;②打回项开始集中在表述、命名、体例——卡面依据已经审干净了。出现任一条,立即放行到 Architect,不再打。

## 7. 体量披露制

- 出卡时给**体量预算**(常规卡默认 ±500 行,项目可调);预授权上限惯例 = 预算 × 1.3(gross)。
- 穷举覆盖型任务(全量 schema/规则表 × 正反测试)应在卡内预授权更高上限或声明不设限——此类卡体量由强制交付面决定。
- 计量口径:**格式化工具处理后**的源码行数;交付报告分组统计:手写代码 / 强制生成物 / fixture 数据 / 协作文档。
- **超预算不停工**,交付报告如实披露,是否重构由 review 裁量。预算是披露口径,不是停工触发器。

## 8. 通用工程规约(默认值,部署时按项目覆盖)

| 项 | 规约 |
| --- | --- |
| 类型安全 | 默认最严格模式;禁隐式 any 类(确需用 unknown 类 + 收窄) |
| 错误处理 | 显式错误类型;跨进程/跨语言边界统一转可序列化错误,不 panic/unwrap |
| 注释/文案 | 语言按项目定(默认简体中文);注释解释"为什么"而非"是什么" |
| 提交 | Conventional Commits(feat/fix/docs/refactor/test/chore + 描述);**尾注执行者身份**,格式见 §8.1——多 agent 同仓提交,审计必需 |
| 分支 | 手工创建时默认 `task/<ID>-短描述`;宿主已分配时沿用实际分支;并行任务不得共享可写分支,同一执行者的顺序批量卡可共用分支 |
| 依赖 | 新增依赖必须卡面明确批准;出卡人枚举官方 companion 闭包(types、插件、CLI、配套包)**并核查各新增包的 peerDependencies**;交付报告列实际新增依赖全表供逐个核对 |
| 跨边界类型 | 单一类型源 + 代码生成;生成物可由固定命令重现;生成物 lint 豁免必须**按具体文件路径显式列名**(禁通配目录、禁行内豁免注释) |
| 平台 | 涉及路径/进程/文件的代码双平台考虑(路径分隔、进程组差异是重灾区) |
| 门禁命令表 | 【部署时填写:类型检查 / lint / 测试 / 格式化 / 生成物重现校验的完整命令】<br>示例(项目 A):`pnpm typecheck && pnpm lint && pnpm test`;`cargo test && cargo clippy -- -D warnings && cargo fmt --check`;bindings 重生成后 `shasum -a 256` 比对零漂移 |

### 8.1 提交署名(谁提交谁署自己那条)

多个 agent 往同一个仓库提交,`git log` 的 author 字段通常是同一个人类账号——**署名是唯一能区分"这段代码出自谁"的凭据**,不是装饰。等到需要追责或复盘"这个决定当时是谁做的"时,它是仅有的线索。

格式:commit 消息末尾加 git trailer,**一行一条**:

```
Co-Authored-By: <角色>-<模型标识>
```

角色名与模型标识在部署时一并确定并记入信道存档(与 §1.2 的模型来源同一件事)。模型标识用该 agent 在本项目里的**固定代号**,不用版本号——版本会变,而署名是历史记录,变了就对不上从前的提交。形如:

```
Co-Authored-By: Architect-<模型代号>
Co-Authored-By: Implementer-<模型代号>
Co-Authored-By: Reviewer-<模型代号>
```

Implementer 与 Reviewer 同源时**代号相同、角色前缀不同**,这正是要区分的两件事:同一个模型在两个角色下的产出,责任归属不同。

**规则**:

- 署名要求作用于**本方新建提交**。已存在的宿主 baseline/checkpoint 和本轮起点按原样继承,不得仅为规范提交说明或尾注而 amend;
- **谁提交谁署自己那条,不代签他人**——代签让审计链失真,而审计链失真的时刻,恰好是你最需要它的时刻;
- 一次提交若确实是多方共同产出(如 Architect 在打回后直接改了 Implementer 的分支再提交),**由提交者列全参与方**,并在提交描述里说清各自改了什么——列全不等于代签,前提是这次提交真的包含对方的产出;
- 收口 merge commit 由 Architect 署自己一条即可;被合入的各 commit 里已有各自署名,不必重复;
- **代行期**(§1.1)照常署代行者本人的角色,不署被代行者——事后要能一眼看出哪些裁决和提交出自代行期;
- 需要 GitHub 识别为 co-author 时补邮箱(`Co-Authored-By: Name <email>`);纯内部审计可省。
