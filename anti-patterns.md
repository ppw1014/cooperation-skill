# 反模式池(历史教训,持续追加)

> 每条:❌ 反模式 → 教训与纠正。带「源案例」的来自项目 A 的实战。
> 追加机制:新项目撞出的教训先记入项目实例,由 Owner 触发回流本母本。

## 出卡侧

- ❌ **一张任务卡塞多个目标**——拆!一卡一个可独立验收的目标;相互依赖的用批量模式(统一分支、按卡独立 commit),不是塞进一张卡。
- ❌ **验收标准写"功能正常"**——不可检查 = 没写。每条 DoD 必须可机器或可人工明确判定。
- ❌ **让 Implementer 自行设计跨模块接口**——接口契约必须 Architect 先定;实现侧对契约只有执行权和上报权。
- ❌ **同一任务卡存在两个措辞版本**(仓库版 vs 派活提示词复制版)——单一事实源:提示词只指向仓库卡面路径。(源案例:项目 A T0.1)
- ❌ **修订卡面任一区块后不做三区一致性自查**——裁决改了契约却没同步放开范围,制造新矛盾。任何裁决/修订落卡后,范围 ⇄ 契约 ⇄ DoD 通读一遍再放行复工。(源案例:项目 A T0.2)
- ❌ **契约中使用不限定作用域的全称量词**("全部/一切/任何…取最新")——量词把仓库既有基线也扫进去。必须显式限定("本卡新增的…");既有基线默认继承,升级走独立任务。(源案例:项目 A T0.2)
- ❌ **卡面引用不存在的数据源**——写"按文件顺序展示"却没核实顺序信息在数据链路(源文件 → 构建 → 运行时)中真实存在;构建工具早已把顺序规范化掉了。每个「顺序/坐标/默认值」引用都要先沿链路确认数据源。(源案例:项目 A T2.2)
- ❌ **拿"某测试变绿"当"生产代码修好"的证据,而那条测试根本不走生产路径**——`#[cfg(test)]` 分叉是最常见的形态:项目 C 的工作区清理缺陷里,判题收尾在测试下调用 `cleanup_with_diagnostic()`、只有 `#[cfg(not(test))]` 才 `drop(workspace)`,而那个诊断函数**自身带 `#[cfg(test)]`**,前一阶段做的重试逻辑整个长在测试专用路径上,生产 `Drop` 至今还是一行静默删除。于是卡面门槛「Windows 上该测试通过」**可以在生产代码一行未改的情况下达成**。出卡时逐条问「这条绿了能证明那段生产代码改了吗」,脱钩就把门槛改成直测生产入口 + 编译期证明两路共用同一函数/常量,并明写「仅有某测试变绿不构成达标」。(源案例:项目 C T3.10,Reviewer 预审查出)
- ❌ **防假绿门槛只用规范输入**——正确实现与错误实现在规范输入下往往同样通过,门槛因此没有区分力。项目 C 的同一张卡要求「落盘值必须来自校验后的归一结果,不能直接回写原始输入」,但门槛只给了规范输入 `python/python.exe`——直接回写原始字符串的错误实现在该输入下三项断言全过。补一个**合法但非规范**的输入(`python//python.exe`)才能区分。构造前先核实它真能走到断言处:能过前置校验、不被上游规范化吞掉。(源案例:项目 C T3.10,Reviewer 预审查出)
- ❌ **要求一个当前架构里没有消费方的接口**——项目 C 的卡面写「上界耗尽后错误码进日志」,而该 crate **没有任何日志 crate**,且 Windows release 下 `windows_subsystem = "windows"` 使 stderr 无处可去。这类要求没有可验收形态,实现方只能自行发明接口,而任意打印都可能被误收为达标。出卡前确认要求的出口**真实存在且可消费**;不存在就明确拆到后续卡并从本卡删除,不要留一条"精神上正确"的 DoD。(源案例:项目 C T3.10,Reviewer 预审查出)
- ❌ **用全局 `git grep` 当机器门槛**——"grep 证明不存在第三处硬编码"听着可验证,实测该文件有 11 处**合法**命中(测试数据、旧记录断言),全局结果不可能为零,人工解释一串命中不构成门槛。判据要收窄到具名函数体或具名符号,让"通过"是机械可判的。(源案例:项目 C T3.10,Reviewer 预审查出)
- ❌ **改完文档不跑仓库自身的格式检查就提交**——出卡方改的文档通常**不在实现方允许范围内**,而"格式全绿"通常是实现方 DoD 的一条。项目 C 出卡方改状态文档时把表格某列改长、没跑 Prettier,实现方开工即撞 `format:check` 红灯:文件不在她范围、工作区对它零差异、两项冻结契约无法同时满足,只能 L1 停工等裁决。**她连修都不能修**。提交前跑一遍格式检查,成本几秒。(源案例:项目 C T3.10 派卡当天)
- ❌ **内容生产任务不给"知识边界闭包"清单**——不列出前置概念闭包,产出必然超纲。
- ❌ **卡面点名技术栈却不枚举依赖闭包**——companion 包(types、插件、CLI)与 peerDependencies 没核查,实现期连环 L1。(源案例:项目 A T0.2 两次)

## 实现侧

- ❌ **"发现更好的方式所以直接改了"**——最严重违规。发现的价值在进入裁决流程;裁决前一律按契约实现。
- ❌ **把不同性质的流程问题混为一类**——已授权依赖引发的 lockfile 变更是"最小必要例外"可继续;但 PR 体量、任务拆分策略不是自动例外,需预拆或明确裁决。例外条款逐条对条件,不凭感觉套用。(源案例:项目 A T0.4)
- ❌ **自检写"应该没问题"**——每条 DoD 要写明验证方法;没验过的如实标注存疑。
- ❌ **存疑点漏报**——宁可多报。review 撞出来的隐瞒成本远高于自报。

## 验收侧

- ❌ **未核数据链路就把疑似缺陷定性为实现侧责任**——先查是不是出卡侧引用了不存在的数据源,再定责。冤枉实现者一次,信任成本抵十张卡。(源案例:项目 A T2.2 概念顺序:review 初判"实现丢了作者顺序",核查后确认构建期顺序早已不存在,是出卡缺陷 + 内容模型缺口)
- ❌ **轻信交付报告的"全绿"不复跑**——本地门禁全量复跑 + 生成物重生成 hash 比对;CI 核实到同一 HEAD。
- ❌ **实施发现无回应地合并**——L2/L3 每条都要有归宿(采纳→文档/任务/挂账;否决→给理由),挂账必须落到 backlog 可见位置。
- ❌ **L1 裁决落地不验忠实性**——裁决过的事项,验收时逐项核对是否按裁决原文实现,不默认"裁决了就等于做对了"。

## review 与验收强度

> 源案例:项目 B 的 B-000(一个只有 Owner 和两个 agent 会改的**本地文档检查脚本**)。它走了 7 轮双 Architect 交叉 review + 2 轮实现打回,实际体量做到预算的 2.3 倍。以下四条都出自这一张卡的复盘。

- ❌ **按"这段代码重不重要"决定验收强度**——正确判据是"它面对谁"。对上面那张卡,验收方构造了"有人在架构文档里插入一个编号相似的诱饵章节来骗过检查"这类反例;技术上全部成立,但**没有人会攻击自己的架构文档**。虚构一个不存在的对手,会让常规卡按对抗卡的成本交付。出卡时就回答"谁会攻击它",答不上来就是常规档(protocol §2.2)。
- ❌ **交叉 review 不设止损**——这个环节没有自然终点:每一轮都能挑出东西,而挑出东西不等于值得挑。上述 7 轮里真正救命的是前 4 轮(假绿判据、规则在基线上必然假红、门禁自证循环),后 3 轮全是表述除虫,其成本超过了它们避免的返工。用 protocol §2.1 的必审四类 + 不审清单 + 四个越线信号收口;**审方和被审方都有义务喊停**。
- ❌ **只改被点名的那处,不扫全文**——契约文档里一处语义变更平均牵动 2–3 处引用(正文、理由括号、验收样例、交叉引用节号)。只改被点名处,会让每一轮 review 都稳定产出下一轮的缺陷,轮次因此不收敛。母版 §1.3 第 6 条的"三区一致性"讲的是范围 ⇄ 契约 ⇄ DoD,这里补的是**同一份文档内部**:改完后按关键词全文搜一遍(数量词、被改动的术语、节号),确认没留下前后打架的旧措辞。
- ❌ **拿未经验证的一次性探针去打回实现**——为了不轻信交付报告而临时写的验证脚本自身没被评审过,**既会假绿也会假红**。上述案例中同一个人的临时脚本连续三次假红:取表格单元格没剥反引号、按 `§5.3` 匹配而实际标题是 `### 5.3`、按 `group` 读字段而实际字段名是 `check`。每一次都差点变成一条错误的打回。下结论前先用已知结果的样本验证探针本身(干净输入 → 零问题;注入已知缺陷 → 能报出)。

## 协作机制侧

- ❌ **L1 裁决只修上报的那张卡**——同因扫描:一次裁决扫全批,同一病因不允许卡第二轮。(源案例:项目 A E1 批)
- ❌ **把"必然配套"决策也留在 L1**——五次停工裁决与实现侧首选 5/5 一致 = 裁决零信息增量,白白付出停工等待。低风险必然配套下放绿区,配披露审计。(源案例:项目 A E1 批复盘,催生绿区制度)
- ❌ **形态 A 下不确认对方现场就 merge / 切分支**——共享工作区里这类操作直接摧毁对方未提交工作。第一铁律。
- ❌ **信道里复述文档内容**——信道只给路径与结论;复述制造第二版本,回到"两个措辞版本"老坑。
- ❌ **`git add` 用了 pathspec 就以为安全,`git commit` 却不带**——`commit` 提交整个 index。形态 A 的 index 可能有他人的信道回执,普通提交会连带收入。提交前核对 cached diff 或显式限定路径;误提交后的处理先按 protocol §3.3 确认拓扑与本轮起点,托管任务不能套用回退重提。(源案例:一次出卡提交连带 733 行信道)
- ❌ **靠锚点匹配或 git 视角决定消息写在哪**——两种写法都会把消息插进文件中间:①"找某句话再在其后插入"的编辑方式,长文件里锚点必然歧义,而每条消息的结尾套话都长得差不多;②**形态 A 下用 git 视角判断末尾**——协议要求"自己的消息不 add",暂存版与 HEAD 版因此**永远短于工作区文件**,照它定位就会写到别人未暂存消息的前面。写入点只能是**工作区文件的物理末尾**;`git diff HEAD --` 只用于读增量,不用于定位。**光靠禁令不够**:§3.1 早就明令禁止模糊匹配写入,仍在同一项目里复发两次(#177、#223),第一次的补救是重发"EOF 权威副本",代价是留下一个重复编号——所以现在配的是**写后自验三条断言**(protocol §3.1),当场能发现,不依赖谁记得住。

## 本地并行 worktree 与托管运行

- **为规范提交说明而 amend 宿主 checkpoint**:SilverShield 的 SWO-152 从 `1456e68b` 开始,随后 amend 为 `52102e91`;两者父提交与 tree 相同,但起点不再是交付提交的祖先,Multica 因而拒绝记录。保留本轮起点及之前的提交,只为新提交执行署名规范,每轮 start/交付 check。(源案例:2026-09-09 核对保留 worktree 的 reflog)
- **把本地 worktree 当 A 或 B**:它不共享工作文件/index,也不需要 push 才能看到提交。按实际 common-dir 判断;C 提交独立消息文件并用 ID/ack 确认,不要等对方 add 本方消息,也不要并发争抢单文件的下一个编号。
- **为验收切换托管分支,或收口时清理平台 worktree**:这会改变本轮身份或毁掉平台尚需记录的现场。评审固定交付 SHA,必要时用自己创建的临时 detached worktree;指定 Architect 串行集成,平台任务资源由平台回收。实际宿主回收效果须另做真实运行验证。

## 三方协作(部署 Reviewer 时)

> 前两条是机制上的必然,后三条是设计危险区。**已有一例实战**:项目 C 部署三方后的第一张高风险卡(T3.10,同时改三个模块)走了卡面预审,Reviewer 一轮提出 5 项硬意见,Architect 逐条核实基线代码后**全部成立**,其中两项核实后比预审描述更严重——两处假绿门槛、一处无消费方的接口要求、一处不可执行的 `git grep` 判据、一处状态措辞冲突(五条已分别记入本文件「出卡侧」)。**这些全部是 Architect 自己出卡时漏的**,且都会在实现完成后才暴露。该案例支持 reviewer.md §0 的判断:与 Architect 异源时,**开工前评审是这个角色的第一价值**——出卡侧缺陷的代价是整个实现周期。

- ❌ **三方仍拿 `git add` 当已读回执**——A 的 staging 是单一状态,表达不了谁读了:只有主收件人 add,抄送方用 `git diff HEAD --`,真正水位线写进消息。B/C 连 index 都不共享,必须使用显式确认,C 按消息 ID ack(protocol §3)。
- ❌ **裁决只回给 Implementer,不抄送 Reviewer**——裁决常改契约。Reviewer 手里还是旧卡面,初审时会**成批误打回**已经按裁决改对的地方,而且每一条看起来都"有卡面依据"。卡面的任何修订同理。
- ❌ **Reviewer 用打回实质改契约**——"卡面这样不好,改成 Y" 越过了 Architect 的契约权,Implementer 收到两个头的指令,终审时又被判定偏离卡面。判据固定:**打回项指得到卡面某一行才成立**;指不到就是卡面问题,上报裁决。**Implementer 收到无依据打回项有权拒绝**——照改就是让契约在信道里被静默改写,这是三方下最隐蔽的契约漂移路径。
- ❌ **给每张卡都上满初审清单**——常驻 Reviewer 比一次性互审更容易犯这个,因为她每张卡都在场。初审强度必须随卡面强度档走(protocol §2.2),常规档不做恶意输入矩阵。这和「交叉 review 不设止损」是同一种失效,只是搬到了交付后。
- ❌ **有了初审就精简终审**——Reviewer 与 Implementer 若是同一模型,直觉盲区是相关的,她放行不等于没问题。终审五步保持全量是**刻意的冗余**;初审台账的用处是"两边结论不一致处最值得深看",不是免跑凭据。
- ❌ **初审漏检与误打回不回流**——没有这条证据链,三个月后没人说得清初审到底拦下了什么,这个角色会静默退化成橡皮图章。终审查出她该查没查到的 → 记漏检;她打回而终审判定不成立 → 记误打回。两类都补进 reviewer.md 清单。
