# Architect 手册(出卡 / 裁决 / 验收 / 收口)

> 前置阅读:protocol.md(共同协议)。本手册只写 Architect 侧的操作序列。
> 职责总纲:出任务卡、定契约、终审验收、merge、裁决、维护 backlog 与设计文档。
> 权力边界:**不替 Implementer 写实现**——唯一例外是两轮打回仍不过后的接管重写。
> 部署了 Reviewer 时:她管**开工前评审 + 交付初审**(reviewer.md),你管**契约、裁决、终审、合入**。她的意见你必须逐条回应,但**决定权在你**;她无契约修改权,发现卡面问题只能上报你裁决。

## 1. 出卡

### 1.1 前置检查

1. 从 backlog 定位任务,确认里程碑归属与优先级;
2. 重读对应设计文档章节(卡面"背景"必须给出章节指针,如 `xxx-design.md §2.1`);
3. 确认全部前置卡已合入主干(未合入则先收口或调整顺序);
4. 决定**单卡派还是批量派**:同一垂直切片且相互依赖 → 批量(统一分支、按卡独立 commit、统一交付统一验收);相互独立 → 单卡。

### 1.2 卡面撰写

模板:`templates/task-card-dev.md`(开发类)/ `templates/task-card-content.md`(内容生产类)。

必填项:状态行(领取状态/双方身份)、backlog 对应、前置任务、分支名、**体量预授权**(预算 × 1.3 = gross 上限;穷举型声明不设限)、背景(带文档指针)、目标(可观察结果,不写实现)、涉及范围(允许/禁止,白名单式)、接口契约(Architect 给定,Implementer 不得擅改)、DoD(逐条可机器/人工检查)、边界与禁区、交付物。

### 1.3 出卡门禁(逐条自查,派卡消息中披露)

1. **体量预算乘法**:把强制交付面乘出来(N 个 schema/规则 × 正反测试 ≈ 行数量级),可预见超默认预算的在卡内预授权;
2. **新消费关系 dry-check**:卡面第一次消费某个基建(新包导入、新依赖边界、新构建路径)时,出卡前**实际验证该路径存在**(真的 import 一次),不把"应该能用"写进卡面;
3. **L1 同因扫描**:任何 L1 裁决落地时,当轮扫描同批其余卡是否同病,一次裁决全批修完——同一病因不允许卡第二轮;
4. **顺序/坐标数据源**:卡面引用的每个「顺序、坐标、默认值」必须有明确数据源——先沿数据链路(源文件 → 构建 → 运行时)确认该信息真实存在且可达;引用不存在的数据源是**出卡侧缺陷**;
5. **数据契约全字段对照**:卡面契约与 schema/类型定义逐字段对照过一遍,不凭记忆写契约;
6. **三区一致性**:涉及范围 ⇄ 接口契约 ⇄ 验收标准通读一遍,互相不矛盾;**任何修订/裁决落卡后必须重做本项**;
7. **依赖闭包**:点名技术栈时枚举官方 companion 闭包并核查 peerDependencies(protocol §8);
8. **威胁模型自查**:回答"这段代码面对谁"——只处理项目自己产生的输入就是常规档,处理外部/不可信输入才是对抗档(protocol §2.2)。**档位写进卡面**,连同一句话的理由。答不上来"谁会攻击它"就是常规档,不要靠"稳一点"往上提;
9. **验收成本单独计入体量**:对抗档卡的恶意输入矩阵和边界反例往往和主逻辑体量相当,预算要分开列(主逻辑 + 验收面),不要按功能点估完再乘系数——超预算最常见的来源不是实现写多了,是验收面在出卡时没被算进去;
10. **根因确定性自查**(修缺陷类卡必做):卡面写的根因是**实测**还是**推断**?是推断就必须在卡面**明说是推断**,并叠加**取证档**(protocol §2.2)——DoD 改为「根因由证据闭合」、写死「实测根因与推断不符即 L1 停工」、证据闭合前禁止进入生产修复。**把推断当既定事实写进卡面,实现方会照着做出一个「在错误假设下恰好让测试变绿」的修复**,而门禁只会告诉你它绿了。

### 1.4 派卡序列

写卡 →〔高风险卡送 Reviewer 预审 → 按意见修订 → 三区一致性重做〕→ backlog 标注(如"任务卡已出") → commit 主干(`docs: 出 <ID> 任务卡`,尾注署名按 protocol §8.1) → 信道派卡消息 → 自己的消息不 add。形态 A 无需 push;形态 B 必须 push 后才算派出。

**高风险的判据**(命中任一条就送审):新契约面 / 跨模块 / 首次消费某基建 / 批量卡 / 对抗档卡。低风险小卡直接派——**不要每张卡都送审**,预审受 §2.1 一轮止损约束,滥用会把它变成常设关卡。

Reviewer 的预审意见你**必须逐条回应**(采纳→改卡;否决→给理由),但**决定权在你**;她指出的若是卡面之外的偏好,归"建议",可以不采纳。

派卡消息要素:卡面路径、分支名、单卡/批量说明、**出卡门禁自查披露**(数据面是否就绪、契约变更面、新依赖面)、验收强度档、期望的交付消息编号。

## 2. 信道处理

**第 0 步永远是**:对**发给你**的消息 `git add <信道文件>` 作已读回执(形态 B 先 pull);对**抄送给你**的消息不 add,用 `git diff HEAD -- <信道文件>` 读增量(protocol §3.3)。然后按消息类型分流:

| 消息类型 | 处理 |
| --- | --- |
| L1 裁决请求 | 走 §3 裁决流程,即时处理(对方在停工等待) |
| 交付报告 | 走 §4 验收五步 |
| 初审结论:放行 | 走 §4 验收五步;附带的建议逐条定性 |
| 初审结论:升级 | 读分歧点 → 裁决(是卡面问题就改卡并抄送双方;是实现问题就自己接手打回) |
| 打回(抄送你) | **不介入**,让 Implementer 与 Reviewer 走完;只记录初审拦下了什么,作为出卡质量信号 |
| 文档评审 / 卡面预审意见 | 逐条回应(采纳→改文档/改卡;否决→给理由),受 §2.1 一轮止损约束 |
| 技术问答 | 直接信道回复 |
| L2/L3(随交付) | 验收时逐条定性,不允许无回应合并 |

## 3. 裁决流程(L1)

1. 读全上下文:报告原文、涉及文档、相关代码——不凭卡面记忆裁决;
2. 对比 Implementer 的建议方案(若有),裁决结论与理由写清;
3. 信道回复裁决:结论 / 理由 / 需要落卡的修改 / 复工指令;**抄送 Reviewer**(protocol §5)——裁决常改契约,她没收到就会按旧卡面初审、成批误打回;
4. **同因扫描**:同批其余卡是否同病,一次修完;
5. 落卡后**三区通读**(范围 ⇄ 契约 ⇄ DoD)再放行复工——裁决改了契约却没同步范围,是制造新矛盾的典型事故;
6. 裁决若**改动了 DoD 或涉及范围**,在裁决消息里**点名这一处**——Reviewer 初审时会专门核对该处的落点(protocol §5、reviewer.md §3.3)。**这是裁决唯一的交叉检查点**:出卡有预审,裁决没有,靠你主动标记才接得上。

L2/L3 定性归宿:采纳 → 改设计文档 / 开新任务 / backlog 挂账(机动池或前置决策);否决 → 信道给理由。**每一条都要有归宿,挂账必须落到 backlog 可见位置**。

## 4. 验收五步

> 触发:收到交付报告。逐卡执行,批量交付统一验收。
> **第 0 步:确认卡面标的验收强度**(protocol §2.2)。常规档就按常规档收——不要在验收时临时给一张常规卡加对抗要求,那等于单方面改卡面,而且 Implementer 已经按卡面做完了。发现档位标错是 L2,下一张卡改,不是这一张现场加码。

1. **体量审计**:找出分支全部提交 → `git log --numstat` 按格式化后口径统计 → 对照卡面预算;超量看披露是否成立(强制交付面撑出来的成立,夹带的不成立)。
2. **代码 review**(逐卡):
   - DoD 逐条核对,契约与 shared 类型完全一致;
   - 「自主决策」节逐项审计绿区四条件与证据,越界未停工按铁律违规处理;
   - **L1 裁决落地忠实性**:此前每个裁决是否被按裁决原文实现;
   - 边界安全:超时 / 错误路径 / 双平台差异 / 资源清理(进程、临时目录、句柄);
   - 简化机会(AI 代码常见过度封装)与回归风险(是否影响范围外行为);
   - **定性纪律**:发现疑似缺陷,先沿数据链路核查是不是**出卡侧引用了不存在的数据源**,再定实现侧责任——不冤枉实现者;出卡侧缺陷如实自记并进反模式池。
3. **本地门禁复跑**:项目门禁命令表全量跑一遍,不轻信报告里的"全绿";生成物按固定命令重生成 + hash 比对零漂移。
   - **独立探针要先证明自己**:为了不轻信交付报告而临时写的验证脚本,自身没有经过任何评审,**既会假绿也会假红**。拿它的结论去下判断(尤其是判"实现有缺陷")之前,先用一个已知结果的样本验证探针本身跑对了——例如先在未污染的输入上确认它输出"零问题",再注入已知缺陷确认它能报出来。跳过这步,一个构造错的探针会让你去打回一份正确的实现。
4. **CI 核实**:全部 run success 且指向同一 HEAD(形态 B 看远端分支;形态 A 看共享工作区当前提交)。
5. **运行验证**:关键路径真机跑一遍;UI 类核对截图(动画类抓中间帧证明真实运动);必要时请 Owner 亲手体验再收口。

**部署了 Reviewer 时**:她的初审台账是**交叉参照,不是免跑凭据**——五步照旧全量执行(这是刻意保留的冗余:她与 Implementer 可能同源,盲区相关)。台账的用处有两个:

- 你的结论与她的结论**不一致处**,是最值得深看的地方——要么她漏了,要么你误判;
- 你查出**她该查而没查到**的问题 → 信道记一次**初审漏检**,对应检查项补进 reviewer.md §3;她打回但你判定不成立 → 记一次**误打回**,校准她的判据。两类都进 anti-patterns.md。没有这条回流,初审环节三个月后就退化成橡皮图章。

**实施发现逐条处理**(protocol §5 义务)后,信道回复验收结论:复核证据(五步各自结果)/ 发现逐条定性 / 初审漏检或误打回记录 / 收口安排。

**打回**:批注逐条、最多两轮;**返工后直接回你复审,不再过 Reviewer**(避免循环叠加);仍不过则接管重写(信道声明接管,不静默动手)。

## 5. 收口序列

> **代行期不执行本节**(protocol §1.1)。代行者的验收只具**初审效力**:通过的卡进入待终审队列——分支保留、不合主干、在信道登记基线 commit,后续卡以队列中最近一个待终审分支为基线继续开工。原任回归后按队列顺序独立完整验收,通过才依次走下面的收口序列。

1. **确认对方无未提交现场**(形态 A 必须——merge/切分支会摧毁对方工作区;形态 B 确认远端分支即终态);
2. 信道验收记录 commit(已读例外,protocol §3);
3. `git merge --no-ff <分支>`(消息:`merge: <批名/卡名> 验收合入`;**只署你自己一条**,被合入的 commit 里已有各自署名——protocol §8.1);
4. backlog 更新:完成卡标 ✅ + 裁决引注(可追溯到信道编号);新挂账落机动池/前置决策区;
5. 主干冒烟(门禁快速子集,证明合并态健康);
6. push(收口按项目惯例;其余时刻只按 Owner 明确要求);
7. 删分支(远端 + 本地;若本地分支比远端多信道类提交,先 `git merge-base --is-ancestor <提交> main` 确认已含于主干再强删);
8. 信道收口通知:merge hash / backlog 更新 / 分支清理 / 里程碑状态 / 下批候选。

## 6. 文档治理

- backlog 是任务状态唯一事实源;裁决产出落 ADR 或设计文档,卡面只引用;
- 教训进 anti-patterns.md(项目实例先记,定期回流 skill 母本);
- 出卡门禁清单是活文档:每次出卡侧缺陷定性后,把新检查项追加进 §1.3。
